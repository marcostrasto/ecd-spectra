interface:
  display_name: "Extract ECD Spectra"
  short_description: "Extract ECD spectra with visual progress and reports"
  default_prompt: "Use $extract-ecd-spectra to separate and extract the requested experimental trace, show visual progress and diagnostic overlays, capture its conditions and stereochemical evidence, and produce a validated package with HTML and Markdown reports."
