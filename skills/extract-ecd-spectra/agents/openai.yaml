interface:
  display_name: "Extract ECD Spectra"
  short_description: "Discover and extract verified experimental ECD curves"
  default_prompt: "Use $extract-ecd-spectra to find experimental ECD curve candidates with unambiguous structure and solvent evidence, ask me to select candidates, then extract confirmed traces with concise visual progress and validated reports."
